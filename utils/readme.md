These utilities can be useful in testing your display

`bmp2rgb.py` - Converts a .bmp file to a 2-byte per pixel RGB565 little-endian `.raw` file that can be blitted directly to the display


`disp.py` - displays a `.raw` file created by `bmp2rgb.py`
